// LAUNCH BLOCKER: Set NEXT_PUBLIC_SITE_URL to the final public domain before launch.
// No custom production domain is currently connected to the Sites project.
export const productionSiteUrl =
  process.env.NEXT_PUBLIC_SITE_URL ?? "https://replace-with-production-domain.invalid";

